apiVersion: apps/v1
kind: Deployment
metadata:
  name: latipe-media-service
spec:
  replicas: 1
  strategy: # change here
    type: RollingUpdate # strategy type is Recreate or RollingUpdate. Default is RollingUpdate
  selector:
    matchLabels:
      app: latipe-media-service
  template:
    metadata:
      labels:
        app: latipe-media-service
    spec:
      containers:
        - image: 0353486363/latipe-media-service:latest
          imagePullPolicy: IfNotPresent
          name: latipe-media-service
          env:
            - name: SETTINGS_FILE
              value: "k8s.json"
          resources:
            requests:
              cpu: 200m
      # restartPolicy: Always


---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: latipe-media-service
spec:
  scaleTargetRef:
    apiVersion: apps/v2
    kind: Deployment
    name: latipe-media-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
